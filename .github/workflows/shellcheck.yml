name: shellcheck

on:
  pull_request:
    paths:
      - "**/*.sh"
      - "install.sh"
      - "scripts/**"
      - "src/**/*.sh"
      - ".github/workflows/shellcheck.yml"
  push:
    branches: [main]
    paths:
      - "**/*.sh"
      - "install.sh"
      - "scripts/**"
      - "src/**/*.sh"

permissions:
  contents: read

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run ShellCheck
        uses: ludeeus/action-shellcheck@00cae500b08a931fb5698e11e79bfbd38e612a38 # 2.0.0
        with:
          severity: error
          ignore_paths: |
            tests
            .plan
          ignore_names: |
            console-help-old-backup.sh
            help-console-old-backup.sh
        env:
          # SC1091: don't follow sourced files (they may not be on the linter's PATH)
          # SC2155: declare-and-assign is fine for our use
          SHELLCHECK_OPTS: "-e SC1091 -e SC2155"
